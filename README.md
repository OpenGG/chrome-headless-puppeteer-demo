# chrome-headless-puppeteer-demo
Use puppeteer to create screen capture of web pages.

## Usage

```bash

# clone
git clone https://github.com/OpenGG/chrome-headless-puppeteer-demo.git

# install
npm install

# run
node index.js

# use: navigate to http://localhost:8000

```
